<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="30dp"
    android:foreground="?selectableItemBackground"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/imgHolder"
        android:layout_width="90dp"
        android:layout_height="90dp"
        android:src="@drawable/album"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:contentDescription="@string/the_picture_of_song" />

    <TextView
        android:id="@+id/nameSinger"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:text="@string/pink_floyd"
        app:layout_constraintBottom_toBottomOf="@id/imgHolder"
        app:layout_constraintLeft_toRightOf="@id/imgHolder"
        app:layout_constraintTop_toBottomOf="@id/songTitle"
        android:textSize="18sp" />

    <TextView
        android:id="@+id/songTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:text="@string/time"
        app:layout_constraintBottom_toTopOf="@id/nameSinger"
        app:layout_constraintLeft_toRightOf="@id/imgHolder"
        android:textSize="18sp"
        app:layout_constraintTop_toTopOf="@id/imgHolder" />


</androidx.constraintlayout.widget.ConstraintLayout>